#include <bits/stdc++.h>
using LL = long long int;
using namespace std;

#define fast                   \
  ios::sync_with_stdio(false); \
  cin.tie(0);                  \
  cout.tie(0);

#define N 100000
#define inf INT_MAX
#define ninf INT_MIN
#define pb push_back

inline void solve() {}

int main() {
#ifndef ONLINE_JUDGE
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
#endif

  fast int nt = 0;
  cin >> nt;
  while (nt--) {
    solve();
  }

  return 0;
}
