#include <bits/stdc++.h>
using namespace std;

#define inf INT_MAX
#define ninf INT_MIN
using LL = long long int;
using pii = pair<int, int>;

void solve(){

}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int nt;
  cin >> nt;
  while (nt--) solve();

  return 0;
}
